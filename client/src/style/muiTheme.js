import * as colors from './colors';
import getMuiTheme from 'material-ui/styles/getMuiTheme';

export default getMuiTheme({
  fontFamily: 'quicksandRegular',
  palette: {
    primary1Color: colors.aquaMarine
  }
});
